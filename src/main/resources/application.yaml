spring:
  data:
    redis:
      password: password
      host: your_redis_host
      port: 10449
      timeout: 10000
      lettuce:
        pool:
          max-active: 10
          max-idle: 5
          min-idle: 1